const jwt = require("jsonwebtoken");


const signToken = async (privateKey, id, name, email, avatar, appId, kid) => {
  try {

  }
  catch (error) {
    console.log(error);
  }
};

module.exports = {
  generateToken
};